<div>
    <h5 class="title is-5 ">Available Sizes</h5>
    <div class="list-item">
        {{#each singleItem.sizes as |size|}}
            <li class="is-size-5">{{size.size_name}}</li>
        {{/each}}
    </div>
</div>